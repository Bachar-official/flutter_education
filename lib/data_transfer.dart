import 'package:flutter/material.dart';

class DataTransferExample extends StatelessWidget {
  const DataTransferExample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
