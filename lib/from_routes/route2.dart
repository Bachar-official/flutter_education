import 'package:flutter/material.dart';

class Route2Example extends StatelessWidget {
  const Route2Example({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Route 2'),
      ),
    );
  }
}
