import 'package:flutter/material.dart';

class Route1Example extends StatelessWidget {
  const Route1Example({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Route1'),
      ),
    );
  }
}
